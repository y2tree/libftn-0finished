#include <stdio.h>
// Écris une fonction qui remplit un tableau avec une valeur
void fill_array(int *arr, int value, size_t n)
{
    // Ton code ici
    int i = 0;
    
    // Utilise size_t pour la boucle
    while ()
    {
        /* code */
    }
    
}

int main()
{
    int arr[5];
    fill_array(arr, 42, 5);
    
    // Vérifie que arr = {42, 42, 42, 42, 42}
    for (size_t i = 0; i < 5; i++)
        printf("%d ", arr[i]);
    
    return 0;
}